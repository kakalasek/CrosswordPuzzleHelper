package org.example.sideframes.removeframe;

import org.example.sideframes.SideFrame;

import javax.swing.*;

public class RemoveFrame extends SideFrame {
    public RemoveFrame(){
        super("remove", new RemovePanel());
    }
}

