package org.example.sideframes.addframe;

import org.example.sideframes.SideFrame;

import javax.swing.*;

public class AddFrame extends SideFrame {
    public AddFrame(){
        super("Add", new AddPanel());
    }
}
